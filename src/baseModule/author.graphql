type Author {
    id: String!
    firstName: String
    lastName: String
    posts: [Post]
}

type Post {
    id: String!
    title: String!
    likes: Int
}

type Query {
    author(id: String!): Author
}
